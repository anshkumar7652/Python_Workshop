from folder import *            #importing all files from the folder
a=int(input('enter number1 to perform the opeerations: '))

b=int(input('enter number2 to perform the opeerations: '))

print(file1.add(a,b))
print(file1.sub(a,b))
print(file2.multiply(a,b))
print(file2.divide(a,b))

